import Extend from 'flarum/common/extenders';
import AvatarDecorationSettingsPage from './components/AvatarDecorationSettingsPage';

export default [new Extend.Admin().page(AvatarDecorationSettingsPage)];
